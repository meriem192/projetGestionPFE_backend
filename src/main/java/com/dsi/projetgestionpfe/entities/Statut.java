package com.dsi.projetgestionpfe.entities;

public enum Statut {
    En_attente,  // Document en attente de traitement
    Accepte,     // Document accepté
    Refuse,      // Document refusé
    Soumis,      // Document soumis pour validation
    Note         // Document avec une note (peut-être une évaluation)
 }
